#pragma once

// local config, specific to each separate installation
namespace NConfig {
    const char* essid         = ;
    const char* wifi_password = ;

    const char* hostname     = ;
    const char* ota_pass_md5 = ;
}
